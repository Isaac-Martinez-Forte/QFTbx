# What the algorithms share: the interval projection onto the Nichols
# chart, the box classification against the boundaries, the nominal
# stability check, the search list and its nodes, the Quick Solution cuts.
qftbx_sources_here(qftbx_core)
