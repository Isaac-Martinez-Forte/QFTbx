# Algorithm MC3: the gain out of the tree. The box holds the zeros and poles;
# the admissible gains of a node are a union of intervals computed exactly
# from the boundary columns, never bisected.
qftbx_sources_here(qftbx_core)
